# Geometric-Galois Bridge

    This note records the mainstream bridge for the canonical-lane package.

    Mainstream precursors:
    - etale fundamental groups, the exact sequence to the absolute Galois group, and anabelian reconstruction results provide the mainstream geometric-Galois package underlying the lane.
- the lane adds admissible-carrier, defect-ledger, and remainder-tracking structure on top of that standard anabelian package.

    Problem-side translation:
    - `P_mc` projects the visible anabelian object to the admissible sector `Y_mc^ANA` tracked by the lane.
    - `Q_rem := I - P_mc` records the explicit remainder `R_ANA` carried by the defect and coherence ledgers.
    - the manuscript closes on `Y_mc^ANA`; any stronger external consequence must control `R_ANA` rather than dropping it.

    Reviewer use:
    - translate the transport language into a decomposition between the admissible sector and the unresolved complement,
    - compare the bridge package to the standard arithmetic/geometric objects rather than reading the lane as disconnected notation.
